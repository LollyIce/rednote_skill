---
name: rednote-skill
description: 小红书平台自动发布文章与高流量内容分析学习 Skill。支持热门话题排行榜、内容质量检查（去AI味/不捏造/情绪化写作）、浏览器自动化抓取与发布。
---

# 小红书 (RedNote) Skill

## 概述

本 Skill 提供三大核心能力：

1. **学习高流量文章** — 搜索并抓取小红书高互动笔记，分析标题模式、标签策略、内容结构
2. **自动发布文章** — 浏览器自动化发布笔记，**发布前自动执行内容质量检查**
3. **热门话题排行榜** — 抓取近期最热话题 Top 10/20，发现蹭热点方向

## 内容质量标准

本 Skill 内置严格的内容质量检查机制，确保发布的内容符合小红书平台调性：

### 🚫 去 AI 味
- 禁止"首先…其次…最后"、"综上所述"、"值得一提的是"等结构化表达
- 句子要短，像发微信一样写，不要像写论文
- 要有个人语气词："说真的"、"怎么说呢"、"就…挺离谱的"

### 🚫 不捏造事实
- 不编造用户未提供的具体时间、价格、地点、人物
- 不伪造"朋友说"、"同事推荐"等他人转述
- 所有具体事实必须来自用户输入，脚本会自动标记可疑信息

### 🎭 自然情绪表达
- 情绪藏在细节里，不直接喊出来：用五感描写代替"太好了"
- 制造反差和转折："本来只是随手拍了一张，没想到…"
- 适当留白：省略号和破折号比感叹号更有力
- 克制比夸张更高级："还行吧"比"绝绝子"更真实
- 连续 3 句以上的感叹会被标记为情绪过密

## 前置要求

- Python 3.9+
- `pip install -r scripts/requirements.txt`
- `python -m playwright install chromium`

## 使用方式

### 1. 首次登录

```bash
cd scripts
python browser_helper.py
```

### 2. 学习高流量文章

```bash
python scripts/analyze_articles.py --keyword "美食推荐" --count 20 --sort hot
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `--keyword` | ✅ | - | 搜索关键词 |
| `--count` | ❌ | 20 | 抓取笔记数量 |
| `--sort` | ❌ | hot | hot(最热) / new(最新) |
| `--output` | ❌ | 自动生成 | 报告输出路径 |

### 3. 自动发布文章

```bash
# 发布前会自动检查内容质量
python scripts/publish_article.py --title "标题" --content "正文" --tags "标签1,标签2" --draft

# 如果内容含有用户确认的真实事实（如价格），通过 --facts 传入避免误报
python scripts/publish_article.py --title "标题" --content-file article.md --facts '{"prices": ["68"], "places": ["xx咖啡馆"]}'

# 跳过质量检查（不推荐）
python scripts/publish_article.py --title "标题" --content "正文" --skip-check
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `--title` | ✅ | - | 文章标题 |
| `--content` / `--content-file` | 二选一 | - | 正文内容或文件 |
| `--tags` | ❌ | - | 标签，逗号分隔 |
| `--cover` | ❌ | - | 封面图路径 |
| `--draft` | ❌ | false | 仅保存草稿 |
| `--skip-check` | ❌ | false | 跳过内容质量检查 |
| `--facts` | ❌ | - | 用户确认的真实事实 JSON |

### 4. 热门话题排行榜

```bash
# 获取 Top 20 热门话题
python scripts/hot_topics.py --count 20

# 获取 Top 10 并指定输出
python scripts/hot_topics.py --count 10 --output trending.md
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `--count` | ❌ | 20 | 排行榜数量 (10/20) |
| `--output` | ❌ | 自动生成 | 报告输出路径 |

### 5. 独立内容检查

```bash
# 单独检查文章质量，不发布
python scripts/content_checker.py --title "标题" --content "正文内容"
python scripts/content_checker.py --title "标题" --content-file article.md
```

## 注意事项

> ⚠️ 小红书没有公开 API，所有操作通过浏览器自动化完成。
> 请勿频繁操作以避免触发风控。脚本内置随机延时模拟人工行为。

## 文件结构

```
rednote_skill/
├── SKILL.md
├── scripts/
│   ├── requirements.txt
│   ├── config.py                # 配置管理
│   ├── utils.py                 # 通用工具
│   ├── browser_helper.py        # 浏览器管理
│   ├── content_checker.py       # 内容质量检查器 ← NEW
│   ├── analyze_articles.py      # 文章分析
│   ├── publish_article.py       # 文章发布（集成质量检查）
│   └── hot_topics.py            # 热门话题排行榜 ← NEW
├── examples/
│   ├── sample_article.md
│   └── analysis_report_sample.md
└── resources/
    ├── selectors.json           # CSS 选择器
    └── writing_guidelines.json  # 写作质量指南 ← NEW
```
